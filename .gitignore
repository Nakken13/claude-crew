# Dépendances / envs
node_modules/
.venv/
venv/
__pycache__/
*.pyc

# Secrets
.env
.env.local
.env.*.local

# Graphify (régénérable via `graphify update .` / `graphify init`)
graphify-out/*
!graphify-out/.gitkeep

# État interne des hooks crew (régénéré, pas de valeur à versionner)
crew/CLAUDE_CONTEXT/.task_state.json
crew/CLAUDE_CONTEXT/.hook_debug_dump.json
crew/CLAUDE_CONTEXT/.batch_locks.json
crew/CLAUDE_CONTEXT/.batch_locks.mutex
crew/CLAUDE_CONTEXT/BATCH_LOCKS.md

# Build outputs courants
dist/
build/
.next/
*.tsbuildinfo

# OS / éditeurs
.DS_Store
Thumbs.db

# Claude Code — état local/perso, jamais partagé entre postes
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/
